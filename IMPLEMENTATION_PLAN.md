# 《甄嬛传大富翁》H5 数字伴侣实施计划

> 执行原则：测试先行，每阶段依次运行 `npm run lint`、`npm run typecheck`、`npm run test`，最终增加 `npm run build`。

## 全局约束

- Master Data 的 26 块地产与五个人物数值原样导入；历史字段 `sale` 映射为 `purchasePrice`，同时作为本次 MVP“卖给银行”的售价。
- 电子骰子为服务端 2d6，单回合一次，结果持久化；物理模式不创建或管理 Turn。
- 所有余额与产权变化必须处于 PostgreSQL 事务，产生不可删除账本；幂等键绑定 canonical request hash，同键不同业务输入必须拒绝。
- WebSocket 只发布通知，连接或重连后客户端重新获取权威快照；历史列表有明确上限且尚未分页。
- 系统不追踪实体棋子位置，地产操作依赖已确认落点。

## 当前交付状态

核心 MVP 已形成可运行闭环：超管页面幂等建房，玩家和银行以设备令牌加入/重连，银行开局/结束，物理或电子骰子推进，玩家声明落点并完成账务、审批和完整地产处置，银行执行停轮/轮次修正与固定目标补偿撤销，玩家和银行工作台从 PostgreSQL 权威快照恢复。尚未实现的扩展规则、运营能力和全链路测试集中记录在 `KNOWN_LIMITATIONS.md`，不与已完成能力混列。

## 阶段 1：工程骨架与数据

- [x] 创建 npm monorepo、TypeScript、ESLint、Vitest、Next.js、Fastify、Prisma 配置。
- [x] 定义 Prisma 核心模型、完整初始 migration、Master Data 校验及事务 seed；补充 `runtime_safety` 的 request hash、append-only 历史保护和骰点 CHECK，以及历史表 `TRUNCATE` 防护链：`003` 建立触发器、`004` 要求 `_test` 库和显式 transaction-local 能力、`005` 将最终能力绑定当前 PostgreSQL 事务 ID。
- [x] 为 2d6、地产数据映射、技能配置编写领域测试并实现。
- [x] 建立 PostgreSQL 双连接集成测试入口。

## 阶段 2：房间、身份与快照

- [x] 实现超管令牌、`Idempotency-Key` 幂等建房、玩家/银行幂等加入、设备令牌恢复和人物占用规则；每位玩家必须选择一个未占用人物。
- [x] 加入结果未知时持久化原请求意图并重放；银行授权码不落盘，局域网普通 HTTP 环境使用 `getRandomValues` 生成设备与幂等键。
- [x] 实现幂等开局/结束、PostgreSQL 房间快照与带令牌校验的 Socket.IO 失效通知；真实覆盖并发开局/加入的合法串行结果。
- [x] 房间结束后拒绝新余额、产权、停轮和撤销写入，同时保留已提交幂等键的结果重放。
- [x] 覆盖银行和玩家设备令牌跨 Prisma 客户端恢复测试。
- [ ] 开放完整规则档案、难度、参与人数和银行模式配置，并实现随机人物和初始地产。

## 阶段 3：账务、审批与地产事务

- [x] 实现转账、银行付款/扣款、起点奖励、不可删除账本和统一请求审批。
- [x] 实现购买、建造、出售建筑、抵押、赎回、卖给银行、玩家交易和过路费。
- [x] 实现活动地产锁、条件扣款、乐观锁、绑定 canonical request hash 的 `Idempotency-Key` 和 `Serializable` PostgreSQL 事务。
- [x] 覆盖真实双连接并发购买、重复审批、余额不足回滚，以及领域层回合、收租和处置前置条件。
- [x] 完成购买、建造、出售建筑、抵押、赎回、卖给银行、玩家交易及落点/回合边界的 PostgreSQL 集成测试矩阵。
- [x] 实体新落点原子失效同一玩家的旧落点，取消关联待审批请求并释放地产锁。
- [x] 非地产事件拒绝携带 `propertyName`，避免伪造地产锁；请求拒绝只允许原成功幂等键重放，终态请求换键处理返回冲突。
- [x] 快照顶层提供实际赎回手续费 `redemptionFee`，玩家项提供由有效停轮来源计算的 `tollCollectionBlocked`，供客户端统一计算赎回支出和禁收租状态。

## 阶段 4：回合、停轮、技能与撤销

- [x] 实现电子 2d6、结束回合、银行以 `Idempotency-Key` 作废骰点/强制下一位；物理模式不创建 Turn 并拒绝轮次接口。
- [x] 实现可叠加停轮来源、冷宫禁收租差异和银行手工逐次扣减。
- [x] 通过 Master Data 配置实现五个人物技能。
- [x] 实现审计、余额/地产修正、快照 `reversalCandidate` 和携带显式 `transactionId` 的固定目标补偿撤销。
- [x] 覆盖骰子持久化、停轮、五技能、补偿撤销及身份恢复测试。
- [x] 作废骰点前拒绝仍有关联已结算请求或过路费的回合，补偿撤销后才允许现场纠正。
- [x] 电子回合存在实际应付且未结算过路费时拒绝结束；抵押、自有、已取消落点和冷宫中地主不产生该阻塞。
- [x] 银行冷宫停轮录入与玩家事件审批共用 Master Data 技能配置，原子执行宜修减轮、500 两补贴、账本和审计。
- [ ] 实现银行离线检测、控制权移交和重新指派。

## 阶段 5：移动端 H5 与交付

- [x] 实现欢迎/加入、玩家工作台、地产、账本、银行审批控制台和超管建房页。
- [x] 加入加载态、危险操作确认、响应式触控目标和宫廷视觉；余额/停轮修正确认显示变更前后值。
- [x] 添加 Docker Compose、环境变量示例、API 路由说明、README 和已知限制。
- [x] 添加玩家、银行和超管 Playwright 界面流程。
- [x] 将银行地产/停轮/轮次修正 API、请求拒绝和操作日志接入工作台。
- [x] 接入银行授权码加入、买方交易确认和银行剧情罚款录入。
- [x] 快照轮询串行化并隔离跨身份旧响应；临时加入失败保留设备令牌、业务输入和幂等键。
- [ ] 接入 Socket.IO 快照失效通知；当前客户端以 5 秒快照轮询降级。
- [ ] 实现伙伴卡放回、剧情资金对象、立冬家宴、破产清算和四种胜利结果。
- [ ] 增加不 mock API 的浏览器端全链路测试。
- [x] 当前已知验收记录：默认 Vitest 70 passed/81 skipped；独立且已完成 migration/seed 的 `_test` 数据库完整 Vitest 151/151、PostgreSQL integration 85/85；mock API 的移动端 Playwright 32/32。数字是最近一次已完成运行的证据，不是对后续任意提交的持续保证。
