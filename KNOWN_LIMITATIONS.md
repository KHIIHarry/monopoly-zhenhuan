# 已知限制与人工裁定

本文只记录当前真实边界。已完成的 Cookie Session、账号房间、控制权、角色交换、不可变结算、实时失效通知和测试说明见 `README.md`。

## 现场裁定

- 系统不追踪实体棋子位置；落点、剧情是否已现场结算和未结构化卡牌效果由玩家或银行声明。
- 系统不识别剧情卡、命运卡和伙伴卡的每张具体卡面；未结构化事件由银行输入金额与原因。
- 物理骰子模式不创建电子 Turn，也不管理当前玩家。落点是否符合真实棋盘仍由现场确认。
- 银行录入的停轮次数只校验为正整数，规则尚未定义统一上限；现场必须使用实体规则的实际次数。

## 实时与部署边界

- 当前生产拓扑只支持一个 API 进程。Socket.IO 使用默认内存 Adapter；在引入 Redis Adapter 或等价共享 Adapter 前，不得把 API 扩为多副本。
- 房间事务提交后由当前 API 进程直接发送失效通知，没有持久化 outbox。进程恰好在数据库提交后、Socket emit 前终止时，该次通知可能丢失；客户端在重连、网络恢复、`pageshow`、页面重新可见或下一次通知/操作后会重新拉取完整快照并收敛。
- 不保存断线期间的增量事件，也不把 Socket 当作历史消息队列。REST snapshot 始终是恢复依据。
- 玩家离线只表示 Socket 不在线，不释放人物、银行席位或房间成员关系。
- 生产必须使用 HTTPS/WSS；HTTP 公网部署无法使用始终为 `Secure` 的 Session Cookie。
- 截至 2026-08-01，`npm audit --omit=dev` 仍报告 Next.js 内置 PostCSS 与可选 Sharp 链上的 3 个 high。当前 H5 不接收用户提供的 CSS、source map 或图片处理输入，也未使用 `next/image`；npm 提供的自动方案会把 Next 破坏性降级到 9.3.3，因此未执行 `audit fix --force`。升级 Next 或其兼容依赖后必须重新审计并移除此项。

## 功能边界

- 建房页开放本次账号房间文档要求的常用配置，但未暴露数据库中的全部规则档案、难度、银行模式、胜利模式和资产估值策略。
- 银行按产品规则没有余额账户。玩家向银行转帐会扣减玩家余额并写入账本，但系统不会展示或累计“银行余额”；实体银行资金仍由现场管理。
- 系统尚未实现所有实体卡牌的结构化向导及破产清算向导；不影响现有资金、地产、回合和结算事务。
- 快照历史尚未分页：账本、已决请求和审计各最多返回最近 100 条，落点最多 30 条；银行界面操作日志只展示最近 20 条。长期房间后续需要游标分页或独立导出。

## 测试边界

- 默认 `npm run test` 在未设置 `TEST_DATABASE_URL` 时跳过需要 PostgreSQL 的集成用例。
- 真实栈 Playwright 门禁是显式 opt-in，并拒绝非 `*_test` 数据库或非 `task7_real_*` 临时 schema；普通 `npm run test:e2e` 主要运行可重复的浏览器夹具。
- 本次验收只覆盖银行 + 两玩家同步、双房间隔离、重连恢复、旧版本拒绝、强退立即失效和幂等不重复改变资金/资产；未因理论极端时序增加新的分布式基础设施测试。
